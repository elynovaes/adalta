import { StyleSheet } from "react-native";

const styleLogo = StyleSheet.create({
  imageContainer: {
    alignItems: 'center',
    marginVertical: 10,
    marginBottom: 20,
  },
  image: {
    transform: [{ scale: 0.8 }],
    width: 270,
    height: 45.62,
  }
});

export default styleLogo;